overwrite: true
schema: "http://localhost:5555/graphql"
documents: "src/**/*.graphql"
generates:
  src/generated/gql/:
    config:
      scalars:
        DateTime: "Date"
    preset: "client"
    plugins: []
