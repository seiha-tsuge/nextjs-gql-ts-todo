import { createStyles } from "@/libraries/mantine/core";

export const useStyles = createStyles((theme) => ({
  main: {
    width: "100%",
    height: "100%",
    backgroundColor: theme.colors.gray[1],
  },
}));
