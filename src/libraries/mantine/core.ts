import {
  createStyles as _createStyles,
  rem as _rem,
  useMantineTheme as _useMantineTheme,
  CSSObject as _CSSObject,
  MantineTheme,
  MantineNumberSize,
  MantineSize,
} from "@mantine/core";

export const createStyles = _createStyles;
export const rem = _rem;
export const useTheme = _useMantineTheme;
export type Theme = MantineTheme;
export type CSSObject = _CSSObject;
export type NumberSize = MantineNumberSize;
export type Size = MantineSize;
